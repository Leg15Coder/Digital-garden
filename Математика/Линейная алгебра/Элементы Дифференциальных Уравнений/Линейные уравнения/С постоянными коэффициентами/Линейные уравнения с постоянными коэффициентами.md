### Теорема об общем решении [[Линейные уравнения|ОЛДУ]] с постоянными коэффициентами (Случай простых корней)

Пусть $\{\lambda_i\}_{i=1}^n$ - попарно различные корни $\chi(\lambda) = 0$. Тогда общее решение ОЛДУ имеет вид:
$$y(x) = \sum_{k=1}^n c_k e^{\lambda_k x}, \quad c_k \in \mathbb{C}$$
#### Доказательство:
1. **Проверка, что $e^{\lambda x}$ — решение:**
   Пусть $y = e^{\lambda x}$, тогда $y^{(k)} = \lambda^k e^{\lambda x}$. Подставляя в уравнение:$$\sum_{k=0}^n a_k \lambda^{n-k} e^{\lambda x} = e^{\lambda x} \chi(\lambda) = 0$$
2. **Линейная независимость:**
   Для доказательства того, что это ФСР, составим определитель в точке $x=0$. Матрица системы будет матрицей Вандермонда $V(\lambda_1, \dots, \lambda_n)$. Так как $\det V = \prod\limits_{i<j} (\lambda_j - \lambda_i) \neq 0$ (корни различны), решения ЛНЗ.

### Теорема об общем решении [[Линейные уравнения|ОЛДУ]] с постоянными коэффициентами (Случай кратных корней)

Пусть $\lambda_1, \dots, \lambda_m$ — попарно различные корни характеристического уравнения с кратностями $k_1, \dots, k_m$ ($\sum k_j = n$). Тогда общее решение:
$$y(x) = \sum_{j=1}^m P_j(x) e^{\lambda_j x}, \quad \deg P_j < k_j$$
#### Доказательство
##### Как выглядит решение
Так как любой многочлен $P_j$ - это сумма мономов вида $x^s$, достаточно показать, что функции $y = x^s e^{\mu x}$ при $s \in \overline{0,k-1}$ являются решениями, если $\mu$ — корень $\chi(\lambda) = 0$ кратности $k$.

1. Перепишем уравнение ОЛДУ через оператор дифференцирования: $\chi(D)y = 0$.
2. Поскольку $\mu$ имеет кратность $k$, характеристический многочлен представим как $\chi(\lambda) = \zeta(\lambda)(\lambda - \mu)^k$, где $\zeta(\mu) \neq 0$.
3. Применяя [[Формула сдвига|формулу сдвига]] $P(D)(e^{\mu x} f(x)) = e^{\mu x} P(D + \mu I) f(x)$, получаем: $\chi(D)(x^s e^{\mu x}) =$ $e^{\mu x} \chi(D + \mu I)(x^s) =$ $e^{\mu x} \zeta(D + \mu I) (D + \mu I - \mu I)^k (x^s) =$ $e^{\mu x} \zeta(D + \mu I) D^k (x^s)$.
4. Так как $s < k$, то $k$-я производная $D^k(x^s) \equiv 0$, следовательно, функция $y$ является решением.

##### Почему других нет (полный базис)
Покажем, что набор функций $y_1 = e^{\lambda_1 x}, y_2 = x e^{\lambda_1 x}, \dots, y_n = x^{k_m-1}e^{\lambda_m x}$ образует базис.
Согласно [[Теорема о существовании и единственности задачи Коши|Т. Коши]], достаточно доказать, что система уравнений для нахождения коэффициентов $c_i$:$$\begin{cases} \sum\limits_{i=1}^n c_i y_i^{}(x_0) = y_0^{} \\ \sum\limits_{i=1}^n c_i y_i'(x_0) = y_0' \\ \dots \\ \sum\limits_{i=1}^n c_i y_i^{(n-1)}(x_0) = y_0^{(n-1)} \end{cases}$$совместна для любого набора начальных данных. Матрица этой системы является аналогом матрицы Вандермонда: $$M = \begin{pmatrix}
y_1(x_0)      & y_2(x_0)    & y_3(x_0)  & \dots & y_n(x_0) \\
y_1^2(x_0)      & y_2^2(x_0)    & y_3^2(x_0)  & \dots & y_n^2(x_0) \\
\vdots & \vdots & \vdots & \ddots& \vdots    \\
y_1^{(n-1)}(x_0)      & y_2^{(n-1)}(x_0)    & y_3^{(n-1)}(x_0)  & \dots & y_n^{(n-1)}(x_0)
\end{pmatrix}$$

Предположим противное: строки $M$ ЛЗ. Тогда существуют такие числа $b_i$, что $\sum\limits_{i=0}^{n-1} y_j^{(i)}(x_0) b_{n-1-i} = 0$.
Введем многочлен $P(\lambda) = \sum\limits_{i=0}^{n-1} b_{n-1-i} \lambda^i$ (степени $< n$). Тогда условие ЛЗ означает: $P(D)(y_j)(x_0) = 0$. Из [[Формула сдвига#Утв. О кратности корня|Утв. о кратности корня]] следует, что каждый $\lambda_j$ должен быть корнем $P(\lambda)$ кратности не менее $k_j$. Тогда суммарное количество корней $P(\lambda)$ с учетом кратностей $\ge \sum k_j = n$. Но степень $P(\lambda) < n$, что невозможно по основной теореме алгебры. Противоречие доказывает ЛНЗ функций и полноту общего решения.

-------------------------------------------------------------------------------
