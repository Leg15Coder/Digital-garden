
- Если коэффициенты $a_k \in \mathbb{R}$, то комплексные корни $\chi(\lambda)=0$ входят сопряженными парами: $\lambda = \alpha \pm i\beta$ (следствие теоремы Безу).

### Критерий вещественности

Решение $y(x) = \sum c_k e^{\lambda_k x}$ является вещественным $\iff$ коэффициенты при сопряженных корнях являются сопряженными: $c_{2i+1} = \bar{c}_{2i+2}$, а при вещественных корнях $c_j \in \mathbb{R}$.

#### Переход к вещественному базису

Для пары корней $\alpha \pm i\beta$ кратности $k$ вместо комплексных функций используются вещественные:
$$e^{\alpha x} \cos(\beta x), x e^{\alpha x} \cos(\beta x), \dots, x^{k-1} e^{\alpha x} \cos(\beta x)$$
$$e^{\alpha x} \sin(\beta x), x e^{\alpha x} \sin(\beta x), \dots, x^{k-1} e^{\alpha x} \sin(\beta x)$$

==Зам.== Общий вид решения через $\text{Re}$ и $\text{Im}$:
$$y(x) = \sum_{j=0}^{m-1} e^{\text{Re}(\lambda_j) x} \left( P_j(x) \cos(\text{Im} \lambda_j x) + Q_j(x) \sin(\text{Im} \lambda_j x) \right) + \sum c_i e^{\lambda_i x}$$

-------------------------------------------------------------------------------
